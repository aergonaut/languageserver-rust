'use babel';

import LanguageserverRustView from '../lib/languageserver-rust-view';

describe('LanguageserverRustView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
